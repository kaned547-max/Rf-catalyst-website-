import React from 'react';\n\nconst WorldClock = () => {\n    const timeZones = ['UTC', 'America/New_York', 'Europe/London', 'Asia/Tokyo', 'Australia/Sydney'];\n    const [time, setTime] = React.useState({});\n\n    React.useEffect(() => {\n        const updateTime = () => {\n            const currentTime = {};\n            timeZones.forEach(zone => {\n                currentTime[zone] = new Intl.DateTimeFormat('en-US', {\n                    timeZone: zone,\n                    hour: '2-digit',\n                    minute: '2-digit',\n                    second: '2-digit',\n                    hour12: false\n                }).format(new Date());\n            });\n            setTime(currentTime);\n        };\n        updateTime();\n        const timer = setInterval(updateTime, 1000);\n        return () => clearInterval(timer);\n    }, []);\n\n    return (\n        <div>\n            <h2>World Clock</h2>\n            <ul>\n                {timeZones.map(zone => (\n                    <li key={zone}>\n                        {zone}: {time[zone]}\n                    </li>\n                ))}\n            </ul>\n        </div>\n    );\n};\n\nexport default WorldClock;\n